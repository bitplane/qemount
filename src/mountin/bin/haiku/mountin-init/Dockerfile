ARG MOUNTIN_BUILDER
FROM ${MOUNTIN_BUILDER}

ARG MOUNTIN_TARGET_ARCH
ENV MOUNTIN_TARGET_ARCH=${MOUNTIN_TARGET_ARCH}

COPY mountin-init.c /mountin-init.c
COPY --chmod=755 build.sh /build.sh
ENTRYPOINT ["/build.sh"]
