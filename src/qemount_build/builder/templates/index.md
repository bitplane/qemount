---
title: Template Builder
provides:
  - docker:builder/templates
---

# Template Builder

Creates tar archives from template directories. Each subdirectory becomes
a `{name}.tar` output in `data/templates/`.
