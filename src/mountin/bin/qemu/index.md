---
title: QEMU Guests
---

# QEMU Guests

Virtual machine images that run inside QEMU. These contain kernels, initramfs,
and tools for mounting filesystems and transporting data back to the host.

Each guest supports a set of formats (filesystems, partition tables) and
transports (9P, serial shell).
