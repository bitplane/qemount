ARG PUREDARWIN_BUILDER
ARG BUILDER
FROM ${PUREDARWIN_BUILDER} AS puredarwin
FROM ${BUILDER}

COPY --from=puredarwin /opt/puredarwin /opt/puredarwin

ARG PUREDARWIN_IMAGE_SIZE
ENV PUREDARWIN_IMAGE_SIZE=${PUREDARWIN_IMAGE_SIZE}

COPY --chmod=755 build.sh /build.sh
ENTRYPOINT ["/build.sh"]
