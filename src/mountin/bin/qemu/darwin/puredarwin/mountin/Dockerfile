ARG MOUNTIN_BUILDER
FROM ${MOUNTIN_BUILDER}

ARG MOUNTIN_PUREDARWIN_IMAGE_SIZE
ENV MOUNTIN_PUREDARWIN_IMAGE_SIZE=${MOUNTIN_PUREDARWIN_IMAGE_SIZE}

COPY --chmod=755 build.sh /build.sh
ENTRYPOINT ["/build.sh"]
