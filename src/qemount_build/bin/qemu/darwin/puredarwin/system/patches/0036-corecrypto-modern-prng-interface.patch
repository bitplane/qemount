diff --git a/src/Kernel/Extensions/corecrypto/algorithms/cckprng.c b/src/Kernel/Extensions/corecrypto/algorithms/cckprng.c
index f3c65ab..343bd97 100644
--- a/src/Kernel/Extensions/corecrypto/algorithms/cckprng.c
+++ b/src/Kernel/Extensions/corecrypto/algorithms/cckprng.c
@@ -11,10 +11,20 @@ void cckprng_init(struct cckprng_ctx *ctx, unsigned max_ngens, size_t entropybuf
 				  const void *nonce) {
 	prngInitialize(&ctx->prng);
 	ctx->bytes_generated = ctx->bytes_since_entropy = 0;
+	ctx->getentropy = NULL;
+	ctx->getentropy_arg = NULL;
 
 	cckprng_reseed(ctx, seed_nbytes, seed);
 }
 
+void cckprng_init_with_getentropy(struct cckprng_ctx *ctx, unsigned max_ngens, size_t seed_nbytes,
+								  const void *seed, size_t nonce_nbytes, const void *nonce,
+								  cckprng_getentropy getentropy, void *getentropy_arg) {
+	cckprng_init(ctx, max_ngens, 0, NULL, NULL, seed_nbytes, seed, nonce_nbytes, nonce);
+	ctx->getentropy = getentropy;
+	ctx->getentropy_arg = getentropy_arg;
+}
+
 void cckprng_initgen(struct cckprng_ctx *ctx, unsigned gen_idx) {
 	// Nothing is needed here.
 }
@@ -26,7 +36,15 @@ void cckprng_reseed(struct cckprng_ctx *ctx, size_t nbytes, const void *seed) {
 }
 
 void cckprng_refresh(struct cckprng_ctx *ctx) {
-	// Nothing is needed here, either.
+	uint8_t entropy[32];
+	size_t entropy_nbytes = sizeof(entropy);
+
+	if (ctx->getentropy != NULL
+		&& ctx->getentropy(&entropy_nbytes, entropy, ctx->getentropy_arg) > 0
+		&& entropy_nbytes > 0) {
+		cckprng_reseed(ctx, entropy_nbytes, entropy);
+		cc_clear(sizeof(entropy), entropy);
+	}
 }
 
 void cckprng_generate(struct cckprng_ctx *ctx, unsigned gen_idx, size_t nbytes, void *out) {
diff --git a/src/Kernel/Extensions/corecrypto/include/corecrypto/cckprng.h b/src/Kernel/Extensions/corecrypto/include/corecrypto/cckprng.h
index 4d558d8..d3e016e 100644
--- a/src/Kernel/Extensions/corecrypto/include/corecrypto/cckprng.h
+++ b/src/Kernel/Extensions/corecrypto/include/corecrypto/cckprng.h
@@ -21,10 +21,16 @@
 
 typedef struct PRNG *PrngRef;
 
+typedef int32_t (*cckprng_getentropy)(size_t *entropy_nbytes,
+                                     void *entropy,
+                                     void *arg);
+
 struct cckprng_ctx {
     PrngRef prng;
     uint64_t bytes_since_entropy;
     uint64_t bytes_generated;
+    cckprng_getentropy getentropy;
+    void *getentropy_arg;
 };
 
 #define CCKPRNG_ENTROPY_INTERVAL (1 << 14)
@@ -276,6 +282,14 @@ struct cckprng_funcs {
     void (*reseed)(struct cckprng_ctx *ctx, size_t nbytes, const void *seed);
     void (*refresh)(struct cckprng_ctx *ctx);
     void (*generate)(struct cckprng_ctx *ctx, unsigned gen_idx, size_t nbytes, void *out);
+    void (*init_with_getentropy)(struct cckprng_ctx *ctx,
+                                 unsigned max_ngens,
+                                 size_t seed_nbytes,
+                                 const void *seed,
+                                 size_t nonce_nbytes,
+                                 const void *nonce,
+                                 cckprng_getentropy getentropy,
+                                 void *getentropy_arg);
 };
 
 /*
@@ -304,6 +318,15 @@ void cckprng_init(struct cckprng_ctx *ctx,
                   size_t nonce_nbytes,
                   const void *nonce);
 
+void cckprng_init_with_getentropy(struct cckprng_ctx *ctx,
+                                  unsigned max_ngens,
+                                  size_t seed_nbytes,
+                                  const void *seed,
+                                  size_t nonce_nbytes,
+                                  const void *nonce,
+                                  cckprng_getentropy getentropy,
+                                  void *getentropy_arg);
+
 /*
   @function cckprng_initgen
   @abstract Initialize an output generator.
diff --git a/src/Kernel/Extensions/corecrypto/kext/cc_kext_main.c b/src/Kernel/Extensions/corecrypto/kext/cc_kext_main.c
index 57b7e94..8652ca4 100644
--- a/src/Kernel/Extensions/corecrypto/kext/cc_kext_main.c
+++ b/src/Kernel/Extensions/corecrypto/kext/cc_kext_main.c
@@ -12,7 +12,8 @@ static const struct cckprng_funcs cc_kprng_fns = {
 	.initgen = cckprng_initgen,
 	.reseed = cckprng_reseed,
 	.refresh = cckprng_refresh,
-	.generate = cckprng_generate
+	.generate = cckprng_generate,
+	.init_with_getentropy = cckprng_init_with_getentropy
 };
 
 static struct cckprng_ctx cc_kprng_ctx = {
@@ -27,11 +28,6 @@ kern_return_t cc_kext_start(kmod_info_t * ki, void *d)
 	if (ret == -1) {
 		printf("warning: corecrypto could not be registered. Did another crypto handler beat us to it?\n");
 	} else {
-		prng_error_status error = prngInitialize(&cc_kprng_ctx.prng);
-		if (error != PRNG_SUCCESS) {
-			panic("prngInitialize() failed with code %d", error);
-		}
-
 		register_and_init_prng(&cc_kprng_ctx, &cc_kprng_fns);
 		printf("corecrypto loaded\n");
 	}
