ARG BUILDER
FROM ${BUILDER}

ARG ARCH
ARG JOBS=1
ARG HAIKU_REVISION
ENV ARCH=${ARCH}
ENV JOBS=${JOBS}
ENV HAIKU_REVISION=${HAIKU_REVISION}

WORKDIR /src/haiku/generated
COPY --chmod=755 build.sh /build.sh
ENTRYPOINT ["/build.sh"]
