diff --git a/src/Kernel/xnu/SETUP/decomment/decomment.c b/src/Kernel/xnu/SETUP/decomment/decomment.c
index b2cceac..3500b36 100644
--- a/src/Kernel/xnu/SETUP/decomment/decomment.c
+++ b/src/Kernel/xnu/SETUP/decomment/decomment.c
@@ -29,8 +29,8 @@
  */
 
 #include <stdio.h>
+#include <stdlib.h>
 #include <ctype.h>      /* for isspace */
-#include <libc.h>
 
 /*
  * State of input scanner.
diff --git a/src/Kernel/xnu/SETUP/installfile/installfile.c b/src/Kernel/xnu/SETUP/installfile/installfile.c
index 8e1c914..4a274f2 100644
--- a/src/Kernel/xnu/SETUP/installfile/installfile.c
+++ b/src/Kernel/xnu/SETUP/installfile/installfile.c
@@ -34,15 +34,50 @@
 #include <sys/param.h>
 #include <sys/time.h>
 
-#include <copyfile.h>
-
 void usage(void);
 
+static const char *program_name;
+
+static int
+copy_file(int srcfd, int dstfd)
+{
+	char buffer[64 * 1024];
+	ssize_t count;
+
+	while ((count = read(srcfd, buffer, sizeof(buffer))) != 0) {
+		ssize_t offset = 0;
+
+		if (count < 0) {
+			if (errno == EINTR) {
+				continue;
+			}
+			return -1;
+		}
+
+		while (offset < count) {
+			ssize_t written = write(dstfd, buffer + offset, count - offset);
+
+			if (written < 0) {
+				if (errno == EINTR) {
+					continue;
+				}
+				return -1;
+			}
+			if (written == 0) {
+				errno = EIO;
+				return -1;
+			}
+			offset += written;
+		}
+	}
+
+	return 0;
+}
+
 int
 main(int argc, char * argv[])
 {
 	struct stat sb;
-	void *mset;
 	mode_t mode;
 	bool gotmode = false;
 	int ch;
@@ -52,6 +87,8 @@ main(int argc, char * argv[])
 	const char *dst = NULL;
 	char dsttmpname[MAXPATHLEN];
 
+	program_name = argv[0];
+
 	while ((ch = getopt(argc, argv, "cSm:")) != -1) {
 		switch (ch) {
 		case 'c':
@@ -59,15 +96,19 @@ main(int argc, char * argv[])
 			/* ignored for compatibility */
 			break;
 		case 'm':
+		{
+			char *end;
+			unsigned long value;
+
 			gotmode = true;
-			mset = setmode(optarg);
-			if (!mset) {
+			errno = 0;
+			value = strtoul(optarg, &end, 8);
+			if (errno != 0 || end == optarg || *end != '\0' || value > 07777) {
 				errx(EX_USAGE, "Unrecognized mode %s", optarg);
 			}
-
-			mode = getmode(mset, 0);
-			free(mset);
+			mode = (mode_t)value;
 			break;
+		}
 		case '?':
 		default:
 			usage();
@@ -105,10 +146,9 @@ main(int argc, char * argv[])
 		err(EX_UNAVAILABLE, "mkstemp(%s)", dsttmpname);
 	}
 
-	ret = fcopyfile(srcfd, dstfd, NULL,
-	    COPYFILE_DATA);
+	ret = copy_file(srcfd, dstfd);
 	if (ret < 0) {
-		err(EX_UNAVAILABLE, "fcopyfile(%s, %s)", src, dsttmpname);
+		err(EX_UNAVAILABLE, "copyfile(%s, %s)", src, dsttmpname);
 	}
 
 	ret = futimes(dstfd, NULL);
@@ -145,6 +185,6 @@ void
 usage(void)
 {
 	fprintf(stderr, "Usage: %s [-c] [-S] [-m <mode>] <src> <dst>\n",
-	    getprogname());
+	    program_name);
 	exit(EX_USAGE);
 }
diff --git a/src/Kernel/xnu/SETUP/json_compilation_db/json_compilation_db.c b/src/Kernel/xnu/SETUP/json_compilation_db/json_compilation_db.c
index eb0dfdb..fe1e8a0 100644
--- a/src/Kernel/xnu/SETUP/json_compilation_db/json_compilation_db.c
+++ b/src/Kernel/xnu/SETUP/json_compilation_db/json_compilation_db.c
@@ -42,6 +42,8 @@
 void usage(void);
 char *escape_string(const char *);
 
+static const char *program_name;
+
 /*
  * We support appending to two databases.
  *
@@ -71,6 +73,8 @@ main(int argc, char * argv[])
 	int i;
 	size_t input_file_len;
 
+	program_name = argv[0];
+
 	if (argc < 5) {
 		usage();
 	}
@@ -165,7 +169,7 @@ main(int argc, char * argv[])
 void
 usage(void)
 {
-	fprintf(stderr, "Usage: %s <json_output> <cwd> <input_file> <compiler> [<invocation> ...]\n", getprogname());
+	fprintf(stderr, "Usage: %s <json_output> <cwd> <input_file> <compiler> [<invocation> ...]\n", program_name);
 	exit(EX_USAGE);
 }
 
diff --git a/src/Kernel/xnu/SETUP/kextsymboltool/kextsymboltool.c b/src/Kernel/xnu/SETUP/kextsymboltool/kextsymboltool.c
index 2954a39..ce6a0c7 100644
--- a/src/Kernel/xnu/SETUP/kextsymboltool/kextsymboltool.c
+++ b/src/Kernel/xnu/SETUP/kextsymboltool/kextsymboltool.c
@@ -20,7 +20,11 @@
  *
  * @APPLE_LICENSE_HEADER_END@
  */
-#include <libc.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <unistd.h>
+#include <fcntl.h>
 #include <errno.h>
 #include <ctype.h>
 
@@ -118,7 +122,7 @@ readFile(const char *path, vm_offset_t * objAddr, vm_size_t * objSize)
 {
 	ToolError err = kErrorFileAccess;
-	int fd;
+	int fd = -1;
 	struct stat stat_buf;
 
 	*objAddr = 0;
 	*objSize = 0;
diff --git a/src/Kernel/xnu/SETUP/replacecontents/replacecontents.c b/src/Kernel/xnu/SETUP/replacecontents/replacecontents.c
index 5b14bc3..b59a452 100644
--- a/src/Kernel/xnu/SETUP/replacecontents/replacecontents.c
+++ b/src/Kernel/xnu/SETUP/replacecontents/replacecontents.c
@@ -37,6 +37,8 @@
 
 void usage(void);
 
+static const char *program_name;
+
 int
 main(int argc, char * argv[])
 {
@@ -49,6 +51,9 @@ main(int argc, char * argv[])
 	const char *dst = NULL;
 	ssize_t readsize, writesize;
 	int i;
+	char *cursor;
+
+	program_name = argv[0];
 
 	if (argc < 2) {
 		usage();
@@ -66,16 +71,15 @@ main(int argc, char * argv[])
 		err(EX_UNAVAILABLE, "malloc() failed");
 	}
 
-	newcontent[0] = '\0';
-
+	cursor = newcontent;
 	for (i = 2; i < argc; i++) {
-		strlcat(newcontent, argv[i], newcontentlength);
-		if (i < argc - 1) {
-			strlcat(newcontent, " ", newcontentlength);
-		} else {
-			strlcat(newcontent, "\n", newcontentlength);
-		}
+		size_t length = strlen(argv[i]);
+
+		memcpy(cursor, argv[i], length);
+		cursor += length;
+		*cursor++ = i < argc - 1 ? ' ' : '\n';
 	}
+	*cursor = '\0';
 
 	dstfd = open(dst, O_RDWR | O_CREAT | O_APPEND, DEFFILEMODE);
 	if (dstfd < 0) {
@@ -144,6 +148,6 @@ void
 usage(void)
 {
 	fprintf(stderr, "Usage: %s <dst> <new> <contents> <...>\n",
-	    getprogname());
+	    program_name);
 	exit(EX_USAGE);
 }
diff --git a/src/Kernel/xnu/SETUP/setsegname/setsegname.c b/src/Kernel/xnu/SETUP/setsegname/setsegname.c
index a0d5d3b..44e3af1 100644
--- a/src/Kernel/xnu/SETUP/setsegname/setsegname.c
+++ b/src/Kernel/xnu/SETUP/setsegname/setsegname.c
@@ -20,7 +20,11 @@
  *
  * @APPLE_LICENSE_HEADER_END@
  */
-#include <libc.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <unistd.h>
+#include <fcntl.h>
 #include <errno.h>
 
 #include <sys/stat.h>
@@ -31,6 +35,8 @@
 
 #include <stdbool.h>
 
+static const char *program_name;
+
 /*********************************************************************
 *********************************************************************/
 static int
@@ -54,7 +60,7 @@ readFile(const char *path, vm_offset_t * objAddr, vm_size_t * objSize)
 {
 	int error = -1;
-	int fd;
+	int fd = -1;
 	struct stat stat_buf;
 
 	*objAddr = 0;
 	*objSize = 0;
@@ -107,7 +113,7 @@ readFile(const char *path, vm_offset_t * objAddr, vm_size_t * objSize)
 static void
 usage(void)
 {
-	fprintf(stderr, "Usage: %s [-s OLDSEGNAME] [-i IGNORESEGNAME] -n NEWSEGNAME input -o output\n", getprogname());
+	fprintf(stderr, "Usage: %s [-s OLDSEGNAME] [-i IGNORESEGNAME] -n NEWSEGNAME input -o output\n", program_name);
 	exit(1);
 }
 
@@ -137,6 +143,7 @@ main(int argc, char * argv[])
 	segname_t             * names = NULL;
 	int                     ch;
 
+	program_name = argv[0];
 
 	while ((ch = getopt(argc, argv, "s:i:n:o:")) != -1) {
 		switch (ch) {
diff --git a/src/Kernel/xnu/bsd/sys/make_posix_availability.sh b/src/Kernel/xnu/bsd/sys/make_posix_availability.sh
index 3fb652b..57f005a 100755
--- a/src/Kernel/xnu/bsd/sys/make_posix_availability.sh
+++ b/src/Kernel/xnu/bsd/sys/make_posix_availability.sh
@@ -22,7 +22,7 @@
 # @APPLE_OSREFERENCE_LICENSE_HEADER_END@
 #
 
-function usage() {
+usage() {
     echo "Usage: $0 <output>" 1>&2
     exit 1
 }
