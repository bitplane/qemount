# Freebsd entrypoints

unlike in Linux, we don't have a way to select -m mode in freebsd. How do we pass parameters to the kernel? Might need a config disk container or something. Needs discussion anyway.
